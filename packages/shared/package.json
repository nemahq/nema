{
  "name": "@nema-io/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --cache --cache-strategy content src/",
    "test": "vitest run"
  },
  "devDependencies": {
    "@nema-io/tsconfig": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
