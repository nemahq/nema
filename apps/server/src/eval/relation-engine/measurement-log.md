# 관계 엔진 측정 일지

> 관계 판정(③ 잇기) 평가의 라운드별 기록 — 무엇을 바꿨고, 숫자가 어떻게 움직였고, 무엇을 발견했나.
> 평가 방식은 [relation-design §5](../../../../../docs/flows/save-engine-v2/relation-design.md), raw 결과(실패 사례 전수)는 같은 폴더의 `results-judgment-*.json`(gitignore, 재실행으로 재생성).
> 측정 대상 = 제품과 동일한 판정 1콜(`prompts/relation-judgment.ts` + gpt-5 standard, `LINKING_REASONING_EFFORT`/`LINKING_TIMEOUT_MS` 미러) + 동일한 게이트(`gateProposals`). 채점은 LLM 심판이 아니라 코드 정확 비교(라벨→골든 id가 통제돼 있어 해석 불필요).

---

## 측정 #1 — 2026-06-14 · 평가셋 첫 가동 + supports 조이기 (NEM-131 첫 슬라이스)

**바꾼 것**: 관계 판정 평가셋 신설(시나리오 10개 = 4종 양성 + 함정 4종[지어낸 supports·헛 충돌·경합→교대 오판·무관계]) + `prompts/relation-judgment.ts`의 supports 절 조이기. 측정은 `--runs 8`(시나리오당 8회, 총 80회)로 드문 FP를 안정적으로 잡았다.

**전후** (supports, 80회 micro):

| 프롬프트 | supports precision | supports recall | supports FP | 비고 |
|---|---|---|---|---|
| baseline (`cfc6413e`) | 0.941 | 1.0 | 2 | FP = 전부 "가격 확정 → 가격표 할일" |
| 조이기 v1 (`016fe7d8`) | 1.0 | **0.75** | 0 | **과조임 — 진짜 supports 8건 죽임, 폐기** |
| 조이기 최종 (`6dea9f9b`) | **1.0** | **1.0** | **0** | 2회 연속 재현(80회×2) |

- **헤드라인**: 지어낸 supports FP **2 → 0**, 진짜 supports 손실 없음(recall 1.0 유지). 게이트 통과(applied) supports FP는 전 구간 0 — 조용히 박힌 가짜 근거는 baseline에도 없었다.

**발견 1 — 조임의 축은 "연결어"가 아니라 "근거인가"다.** v1은 "본문에 because/이유는 같은 명시 연결어가 있을 때만 supports"로 조였더니, 근거와 결정이 *다른 진술*에 떨어져 연결어가 없는 정당한 supports(예: "토스가 출시 빠르다" → "토스로 한다")를 8/8 전부 놓쳤다(recall 0.75). 최종본은 "`from`이 `to`의 *근거*로서 논거가 되는가"로 바꿔 — 연결어 없이 떨어져 있어도 인정 — recall을 되살리며 FP만 제거했다.

**발견 2 — 살아남은 FP는 전부 supports→todo였다.** baseline·v2에서 끈질긴 단 하나의 supports FP는 "가격 확정(claim) → 가격표 넣기(**todo**)"였다. 근거는 *주장(claim)*을 받치는 것이고 할 일·질문은 `resolves`로 닫히지 supports로 받쳐지지 않는다(§5: 근거→결정). "`to`는 claim이어야 한다 — task·question은 supports 금지" 규칙을 더하자 이 FP가 사라졌다(시드의 진짜 supports 4건은 모두 claim을 향해 recall 무손실).

**범위 밖(관측만)**: conflicts FP가 80회당 1~4건 출렁였다 — ① 도그푸딩 시나리오의 수수료 단점(e1)을 전환 결정과 "충돌"로 과발화, ② 리디자인↔이탈 인접성을 충돌로 오판. 전부 게이트 pending(사람이 거름)이라 supports applied FP만큼 해롭지 않고, 이번 슬라이스(supports 1순위)의 경계 밖이다. conflicts 조이기는 후속 거리.

**한계**: 시드는 손으로 짠 10 시나리오라 골든이 닫힌 세계다 — 엔진이 골든 밖 "진짜" 관계를 내면 FP로 깎인다. 실패 사례를 원문째 남겨(`results-judgment-*.json`) 사람이 검토·보정하는 루프 전제(eval-design 결정 #6·#7). brain 이주(첫 대량 도그푸딩) 때 시드가 실데이터로 자란다(relation-design §11).

---

## 측정 #2 — 2026-06-15 · conflicts 조이기 (NEM-131 후속 슬라이스)

**바꾼 것**: 측정 #1의 범위 밖 관측(수수료 단점→충돌 과발화)을 본 작업으로 조였다. `prompts/relation-judgment.ts`에 "Conflicts — a contradiction, not a caveat" 절 신설(동시-참 테스트·의미 판단[부정어 불요]·끝점 규칙) + caveat을 `conflicts:false`로 가르치던 기존 예시 수정. 시드에 conflicts 함정 3종 추가(caveat·질문 재유입·바꿀 할 일). 측정은 `--runs 8`(드문 FP 안정화, #1과 동일 자).

**전후** (conflicts):

| 프롬프트 / 시드 | conflicts precision | conflicts recall | conflicts FP | 비고 |
|---|---|---|---|---|
| baseline (`6dea9f9b`, 구 시드 10, 80회) | 0.882 | 0.938 | 2 | FP = 전부 "수수료 단점 ⚡ 토스→포트원 전환" |
| 조임 v1 (`66f7c38b`, 신 시드 13, 104회) | 1.0 | **0.542** | 0 | **과조임 — conflicts-clean(claim↔claim)이 replaces로 샘(진짜 회귀 3/8); 놓친 11 중 8은 change-todo의 과한 골든(발견 3). 폐기** |
| 조임 최종 (`43c612a3`, 신 시드 13, 104회) | **1.0** | **1.0** | **0** | 전 종류 1.0/FP 0 (supports·replaces·resolves 무회귀) |

- **헤드라인**: 헛 충돌 FP **2 → 0**, 진짜 충돌 손실 없음(recall 0.938 → 1.0). conflicts는 게이트가 늘 pending이라 applied FP는 전 구간 0(조용히 박힌 거짓 충돌은 원래 없음) — 가치는 검토함에 올라오는 거짓 충돌 노이즈를 줄이는 것. 동일 자 증거: 구·신 시드 공통 시나리오(`invented-supports-dogfood`의 수수료 caveat)에서 FP가 사라졌다.

**발견 1 — 프롬프트의 예시가 FP를 가르치고 있었다.** 기존 예시가 caveat("포트원 정산 리포트 약함")을 `{ type: conflicts, confident: false }`로 내보내라 시키고 있었다 — 도그푸딩 헛 충돌의 직접 뿌리. 이 줄을 "caveat은 무관계(둘 다 참이라 충돌 아님), 침묵하라"로 고친 게 가장 큰 레버.

**발견 2 — 과조임의 축은 supports 전례와 동형(#1 발견 1과 짝).** v1은 "conflicts는 과발화된다, 의심하라" 톤으로 절을 키웠더니, 진짜 claim↔claim 충돌(자체구현 vs Supabase Auth)을 replaces로 내리거나 침묵시켜 8회 중 3회 놓쳤다 — 충돌을 살리려는 의도 자체가 톤에 눌린 것(`torn→conflicts` 한 규칙만이 아니라 replaces로 새는 길과 침묵 둘 다). v1의 recall 0.54에는 이 진짜 회귀와, 골든 자체가 과했던 change-todo(발견 3, 모델은 옳게 침묵)가 섞여 있다. 최종본은 "caveat만 도려내되 **진짜 상호배타는 충돌로 살려라 — 교체·침묵으로 내리지 마라**"를 명시해 claim↔claim 회귀를 걷어냈다(recall 1.0). 조임의 레버는 "충돌 의심"이 아니라 "동시에 참이 되나"라는 한 칼금.

**발견 3 — 할 일(todo)은 미래 의도라 현재 충돌을 안 만든다.** "10MB로 결정"(claim) vs "50MB로 올리는 작업"(todo)을 v1에서 모델이 8/8 충돌로 오발할 거라 봤으나, 실제로는 8/8 **충돌로 안 봤다** — 할 일은 "바꿀 계획"이지 "지금 X가 참"이라는 주장이 아니라 결정과 *지금은* 둘 다 참(기껏해야 미래 replaces 씨앗). 핑퐁에서 세운 claim↔todo 양성 골든이 과한 주장이었다 → 해당 시나리오를 false-conflict 함정(golden 빈)으로 보정. 측정 #1 발견 2("supports의 `to`는 claim이어야")와 같은 결의 데이터 교훈 — 끝점 종류가 관계의 성립을 가른다.

**발견 4 — 질문은 충돌 끝점이 못 된다.** 결정된 주제에 질문이 재유입("결제 PG 뭘로?")되면, 엔진은 8/8 **resolves로 닫고 충돌은 0발**했다. 질문은 아무것도 주장하지 않아 결정과 부딪힐 수 없다(닫히거나 무관계).

**한계**: 시드가 13 시나리오 닫힌 세계라 전 종류 1.0은 시드가 작은 탓도 있다 — 자랑이 아니라 "방향이 맞다"의 작은 증거다. baseline은 구 시드(conflicts 골든 2/run), 최종은 신 시드(2/run + change-todo는 음성)라 conflicts 총 모수가 같진 않다(공통 시나리오로 동일 자 비교를 받친다). 이 프롬프트는 prod 워커가 그대로 쓰므로(③ 잇기) FP 감소가 곧 검토함 노이즈 감소다. 실데이터 보정은 brain 이주 때(relation-design §11).

---

## 측정 #3 — 2026-06-17 · 후보 좁히기 K·하한 보정 (NEM-165)

> 앞 측정들과 **다른 단계**다. #1·#2는 ③ 판정(쌍을 직접 주고 옳게 판정하나), 이건 그 앞 ⓐ 후보 좁히기(어떤 쌍을 판정에 넘기나)다. 러너도 `run-candidate-retrieval.ts`로 따로, 채점은 코드 정확 비교. 측정 대상 = 제품과 동일한 `searchNeighbors`(Voyage 임베딩 위 point 최근접). raw 결과는 `results-candidate-retrieval-*.json`(gitignore).

**왜**: ③ 판정은 #1·#2에서 견고함을 확인(쌍 직접 주면 충돌 recall 1·거짓 supports FP 0)했는데, 하니스 실데이터에선 충돌을 놓치고 supports가 과했다. 두 경우 차이가 ⓐ 후보 좁히기 한 곳뿐이라, 범인을 거기로 좁혀(NEM-165) K·하한을 실데이터로 잰다. relation-design §11이 "데이터로 보정"이라 미뤄둔 두 임시값.

**코퍼스**: harness-scenarios.md 글 A~D 본문을 프로덕션 추출(gpt-5)로 1회 통과시킨 실제 진술 스냅샷(55개, ㉠ 방식 — 합성 골든이 아니라 앱이 뱉은 표현 위에서 재 추출 표현차가 벡터 거리에 주는 영향까지 잡는다). 투입 순서(A→B→C→D) 재현 — 새 진술 질의 시점에 앞 글들만 코퍼스에 있다. 골든 쌍은 harness 관계 정답을 이 스냅샷의 실제 id에 매핑(cross-article 6쌍 + 함정 3쌍). 같은 글 형제 쌍은 ⓑ 몫이라 제외.

**바꾼 것**: `worker.ts` `CANDIDATE_TOP_K` **10 → 7**. `CANDIDATE_SCORE_THRESHOLD`는 0.5 유지.

**측정** (하한 0.5 고정, K 스윕):

| K | gold recall | 충돌 검출 | 함정 surfacing | 총 후보쌍 |
|---|---|---|---|---|
| 5 | 0.833 | ✓ | 0/3 | 82 |
| **7 (채택)** | **0.833** | **✓** | **0/3** | **116** |
| 10 (구값) | 0.833 | ✓ | 2/3 | 163 |
| 20 | 0.833 | ✓ | 3/3 | 289 |

- **헤드라인**: 진짜 관계 쌍은 이웃 순위 ≤3에 몰리고(점수 0.55~0.76), 우연한 주제 이웃(거짓충돌·near-dup 함정)은 순위 ≥8에 떨어진다(점수 0.62~0.64). 그 사이 빈 구간 끝인 **K=7**이 함정을 후보에서 빼면서(2/3 → 0/3) 진짜 관계는 그대로 잡고, 코퍼스가 커져 파트너 순위가 밀릴 여유를 남긴다.

**발견 1 — 충돌은 멀지 않았다.** §11이 가장 걱정한 "충돌 쌍이 얼마나 먼지"가 B.15("강자 없다")→C.2("두잇이 선점")로 **순위 2·점수 0.554**. 베이스라인에서도 멀쩡히 잡힌다 — 하니스의 충돌 미검출은 거리 문제가 아니었다(투입 타이밍 = NEM-164, 또는 추출이 충돌 진술을 이유절에 녹임 = NEM-162 결).

**발견 2 — 하한 0.5는 충돌이 박은 바닥선.** 충돌 쌍이 0.554라 하한을 0.6으로 올리면 충돌이 탈락(recall 0.833 → 0.667). 0.3~0.4로 내려도 recall은 그대로고 후보만 는다. 하한은 두 진술만의 유사도라 코퍼스 크기와 무관 — 의미에 박힌 값이라 전역 고정이 맞다. K만 밀도에 민감하다.

**발견 3 — 일반↔특정 뒷받침은 벡터 retrieval 사각.** A.15("성공엔 습관 형성 필요", 일반 원리)→D.7("N잡은 습관 덕에 해볼 만", 특정 사례)이 순위 30·점수 0.573. K를 30까지 키워야 잡히는데 그건 후보 폭발이다. recall 상한이 0.833(6쌍 중 5)인 이유 — K·하한으로 못 푼다. §11의 후속 통로(글자 매치·그래프 이웃) 몫.

**한계**: 코퍼스가 55진술·단일 주제로 작다. "순위 ≤3 vs ≥8" 분리는 의미 구조(진짜 관계가 우연한 주제 이웃보다 가깝다)라 비교적 견고하지만, 절대 K값은 실데이터로 재보정 대상이다. near-dup이 밀도를 빽빽하게 만들어 진짜 파트너를 밀어내는 문제의 근본 해결은 합치기(NEM-162)다 — 165는 손잡이로 누를 데까지, 162가 구조로 없앤다. 러너를 자산으로 남겨 코퍼스가 쌓이면 재측정한다.

---

## 측정 #4 — 2026-06-18 · 같음(중복) 검출을 판정에 합침 (NEM-162 검출 슬라이스)

**바꾼 것**: `relation-judgment` 프롬프트에 엄격 "같음" 절 신설(순수 재진술만, 확신 progression·이유 부연·토픽겹침은 같음 아님) + 스키마에 `duplicates: [{duplicate, of}]`를 `relations`와 나란히 추가. 같음은 관계가 아니라 별도 출력(㉡) — 한쪽을 가려 합치는 동작이라. 검출은 기존 판정 1콜이 겸한다(㉠ — 콜 추가 0). seed에 near-dup 3종 `expectedDuplicates` 골든. 측정 `--runs 8`(#2·#3과 동일 자).

**전후** (합친 프롬프트 vs 측정 #2 베이스라인, 둘 다 18 시나리오):

| 지표 | 베이스라인(같음 절 없음) | 같음 절 추가 |
|---|---|---|
| 중복 precision/recall | — (검출 없음) | **1.0 / 1.0** |
| 중복 과합치(falseMerges) | — | **0** |
| supports FP | 10 | **6** |
| replaces FP | 1 | **0** |
| conflicts FP | 1 | 1 |
| 4종 recall | 1.0 | 1.0 |

- **헤드라인**: 같음 검출 정밀도·재현율 1.0, **과합치 0**. 동시에 4종 관계가 안 깎였다 — 오히려 supports FP 10→6, replaces 1→0. 전엔 약한 supports로 새던 near-dup 쌍(특히 harness-c3d1)이 이제 옳게 빠진 덕(같음으로도 아니고 침묵으로).

**발견 1 — 한 콜에 합쳐도 4종이 안 흔들린다(㉠ 확정).** 개념 5개(4관계+같음)를 한 판정에 굴려도 4종 precision이 유지·개선. 별도 콜(㉡)의 2배 비용을 들일 이유가 측정으로 사라졌다. "같음을 못 가를까"가 이 기능의 최대 리스크였는데, 합친 프롬프트에서도 1.0이라 해소.

**발견 2 — progression은 합치지 않는다(과합치 0의 핵심).** near-dup-harness-c3d1("가장 앞선다" guess → "확정" certain)은 같은 방향이나 확신만 오른 progression이라 골든=합치면 안 됨. 모델이 8/8 중복으로 안 봤다. 엄격 경계(순수 재진술만)가 모델 행동과 일치 — 측정 #3 후속 probe(정밀도 1.0)와 같은 결.

**범위**: 이 슬라이스는 **검출까지**다. 워커는 `duplicates`를 받아도 *합치지 않는다* — merge 변경셋·출처 접기·가리기·마이그레이션은 다음 동작 슬라이스. 즉 prod 동작 불변(판정이 같음을 함께 내지만 소비처 없음), 4종 회귀 없음이 그 안전을 받친다.

---

## 측정 #5 — 2026-06-18 · 재투입 배치 — 가정 반증 + 틀 메아리 과합치 가드 (NEM-162 도그푸딩 후속)

**왜**: 도그푸딩에서 글 D를 통째로 재투입하자 active 관계에 같음·거의같음 진술 간 `replaces`가 ~9개 쌓였다(글자 동일 쌍 포함). 인수인계 가설: "격리 쌍 probe(precision 1.0)가 실배치를 과대평가했다 — 밀집 배치에서 모델이 재진술을 same 대신 replaces로 민다." 이 가설을 측정으로 검증하고, 맞으면 판정 프롬프트에 same↔replaces 가드를 더하려 했다.

**바꾼 것**: 재투입 배치 시나리오 신설(`reingest-batch-article-d` — staging 실스냅샷, 글 D 첫투입=existing·재투입=new). 측정 후 **판정 프롬프트의 "Same" 절에 틀 메아리 가드** 1줄 추가("틀·주어만 같고 내용 다르면 같음 아님" — supports 절에 이미 있던 가드를 같음 절에도). 측정 `--runs 8`.

**전후** (배치 시나리오 + 19 시나리오 전체):

| 지표 | 가드 전 | 가드 후 |
|---|---|---|
| 위험 과합치(falseMerges, 틀 메아리·같은-주어) | 1~3/run | **0** |
| 깨끗한 재진술 합치(duplicate recall, 7쌍) | 1.0 | 1.0 |
| replaces FP (배치) | **0** (전 후보셋·~20런) | 0 |
| 4종 recall / FP | 1.0 / 0 | 1.0 / 0 |

**발견 1 — 인수인계 가설이 틀렸다(현 프롬프트엔 재투입→replaces 결함이 없다).** 깨끗한 후보 13개, 풀코퍼스 49개, 워커 실 retrieval 29개 — 세 조건 ~20런 통틀어 배치 replaces FP는 **0**이었고 글자 동일 트윈을 매번 올바르게 합쳤다. 도그푸딩의 replaces 9는 **"같음" 절 배포 전 옛 프롬프트가 남긴 잔재**였다: 그 잇기 콜(timestamp 단일)은 duplicate를 0개 냈는데(같음 개념 부재의 흔적), 같은 프롬프트로 eval은 매번 ~7개를 합친다. "격리가 실배치를 과대평가"라는 직관은 맞았으나 **축이 replaces가 아니었다.**

**발견 2 — 진짜 밀집-배치 위험은 "틀 메아리 과합치"다.** 밀집 배치에서만(격리 probe는 precision 1.0이라 못 잡던) 모델이 "불리한 이유는 세 가지다"와 "병행하지 않는 이유는 세 가지다"를 — "…이유는 세 가지다" 틀만 같고 세는 대상이 다른데 — 가끔 같음으로 뭉갰다(실 retrieval 12런 중 1, "둘째는 건강"="건강 2순위"형까지 2/12). **같음엔 확신 게이트가 없어 무조건 archive**라, 빈도는 낮아도 무소음·무검토 데이터 손실 = NEM-162가 가장 경계한 과합치. supports 절엔 이미 있던 틀-메아리 가드를 같음 절에 복제하니 **과합치 0**, 깨끗한 합치·4종 무손실(near-dup-harness-c3d1 supports도 8→7로 외려 줄어 무회귀 확인).

**발견 3 — golden=[]은 재투입을 깨끗이 못 잰다(시나리오 한계로 명시).** 재투입 진술들은 글 D 내부의 진짜 supports("건강 안 사라짐"→"건강 2순위" 등)를 다시 찾으므로 배치 시나리오가 supports FP ~29를 띄운다. 지어냄이 아니라 골든을 비워둔 탓이며(merge 시 archive로 자가정리), 이 시나리오가 지키는 메트릭은 **falseMerges=0·replaces FP=0**으로 좁힌다. keeper가 유동적인 추출-뭉침 진술(1 새 진술 = 옛 2~3개)은 falseMerges를 흐려 시나리오에서 뺐다.

**범위**: 프롬프트 변경은 같음 절 가드 1줄(같음↔관계 가르기엔 무영향, 4종 무회귀). 도그푸딩 잔재 replaces(staging 한 스페이스)는 코드 결함이 아니라 옛 프롬프트 데이터 — 재링크 시 합쳐진다(데이터 정리, 본 PR 범위 밖).

---

## 진단 노트 — 2026-06-19 · 라이브 관계 품질의 뿌리 = 추출 자르기 변동 (빌드 없음, 후속 튜닝 세션용)

> 측정 #5 재테스트에서 "관계 그래프가 거칠다"(충돌 미검출·supports 과연결)를 보고, 원인을 단계별로 좁힌 진단만 남긴다. **코드 변경 없음** — 튜닝은 목표선을 먼저 긋고 전용 세션에서.

**증상**: 하니스 줄기를 staging에 재투입하니, 심어둔 충돌(B5 "1인가구 강자 없다" ↔ C "두잇 선점")이 active 관계에 **0건**. supports는 과다(글 A 결론으로 몰림 + 틀 메아리 1건).

**단계별 분리 (충돌 미검출 기준)**:
1. **retrieval ✅** — B5가 C 진술의 후보로 순위 2(점수 0.59)에 올라 판정 프롬프트에 들어간다. 문제 아님.
2. **judgment(밀집) ❌** — B5가 프롬프트에 있는데도 충돌 0/8런.
3. **judgment(격리) ❌** — 그 라이브 쌍 둘만 줘도 0/8런.
4. **well-formed vs 라이브 조각 A/B ⇒ 추출이 범인** — 손으로 주어를 박은 쌍("**1인가구 시장은** 넓은데 강자 없다" ↔ "**1인가구는** 두잇이 선점")은 **충돌 8/8**. 라이브 조각("**시장은** 넓은데…" ↔ "두잇이…**있고**", 주어 탈락·문장 중간 절단)은 **0/8**. 같은 프롬프트·같은 모델.

**그런데 결함이 아니라 변동이다**: 모델·프롬프트는 staging=eval 동일(extractStatements = gpt-5 standard·effort low; 추출 프롬프트엔 이미 "쪼갤 때 주어 반복" 규칙 있음). 현 프롬프트로 글 B·C를 새로 6런씩 추출하면 주어 유지 ~6/6(조각 거의 없음). staging 그 런은 **gpt-5가 자르기 전략을 흔들어**(이유로 묶기 ↔ 절마다 쪼개기) 주어 흘린 **저빈도 나쁜 draw**에 걸린 것. prod/staging 환경 차이도, 모델 차이도 아님.

**천장 vs 고칠 것**: 자르기 전략 변동 자체는 못 없앤다(천장). 주어 탈락은 그 저빈도 증상이라 프롬프트로 *비율*은 낮출 수 있으나 0은 불가. 충돌 1회 미검출 = 데이터 손실 아님(검토행) → "흡수(버킷 B)" 후보.

**후속 세션 진입점 (포크)**:
- (B) **빈도부터 핀** — 글 B/C 추출 20~30런으로 주어탈락률 측정. ~3-5%면 흡수, ~15%+면 (A).
- (A) **추출 프롬프트 조이기** — split 시 주어 유지를 강화(이미 낮은 비율이라 개선 측정에 대량 런 필요).
- (C) **흡수** — 저빈도·회복가능으로 인정, 게이트/검토로 받기.
- supports 과연결도 같은 뿌리(주어 없는 조각이 토픽 이웃에 헐겁게 붙음) 가능성 — 함께 잴 것.

**전제**: 부분 반응형 튜닝 대신, 추출↔retrieval↔관계가 얽힌 루프를 baseline 깔고 통으로 보며, 시작 시 "충분히 좋다"의 목표선 + "튜닝 vs 흡수" 경계를 먼저 긋는다.

---

## 측정 #6 — 2026-06-23 · NEM-168 baseline — 측정 모델 `gemini-3.1-pro-preview` 첫 측정 (엔진 변경 없음)

**바꾼 것**: 프롬프트·게이트·시나리오 그대로. NEM-168 baseline 한 컷으로, 관계 판정을 처음으로 Vertex `gemini-3.1-pro-preview`로 쟀다(가장 추론 무거운 칸이라 무료 중 최강 모델 배정). 결과 JSON에 `model` 박제. 시나리오 19개 × 3회 = 57런.

| 종류 | precision | recall | FP | 비고 |
|---|---|---|---|---|
| supports | 1.0 | 1.0 | 0 | |
| conflicts | 1.0 | 1.0 | 0 | |
| resolves | 1.0 | 1.0 | 0 | |
| **replaces (밀어냄)** | **0.75** | 1.0 | **3** | 전부 applied — 아래 |
| 중복(같음) | 1.0 | 1.0 | 0 | 오병합 0 |
| 전체 | 0.933 | 1.0 | 3 | F1 0.966 |

**발견**:

- **recall 전부 1.0** — 진짜 관계는 하나도 안 놓친다. 틀림은 놓침이 아니라 과연결 쪽이다.
- **밀어냄만 결함, 그리고 제일 위험한 종류다**: applied FP 3건이 전부 같은 `near-dup` 시나리오, 3/3런 동일(우연 아닌 체계). "검증 대상을 N잡으로 **확정했다**" ↔ "남은 후보 중 N잡이 가장 앞선다고 **본다**"를 밀어냄으로 판정해 자동 적용했다. 둘은 같은 결론(N잡)을 확신만 달리한 근접 중복인데, 굳히기를 갈아치움으로 혼동했다. 멀쩡한 추측 진술이 "지난 것"으로 조용히 묻힌다.
- **진단노트(6/19)와 뿌리가 다르다**: 그건 라이브 조각(주어 탈락)이 원인인 충돌 미검출이었다. 이건 well-formed 골든에서 나는 판정 자체의 밀어냄↔근접중복 경계 결함이라, 추출 자르기와 무관하고 pro에서도 재현된다.
- **라벨 주의**: "확정이 추측을 supersede한다"고 우길 여지는 있다(경계 사례). 하지만 결론이 그대로인데 한쪽을 자동으로 묻는 동작은 라벨 논쟁과 무관하게 위험하다.

**다음 (NEM-168)**: 밀어냄↔근접중복 경계가 튜닝/흡수 판단의 1순위 후보. 프롬프트의 replaces 정의 또는 `confident` 임계로 비율을 낮출 수 있을지부터 본다. 수치는 작은 시험지(시나리오 19) 기준 잠정이다.
